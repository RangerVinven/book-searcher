import React    from "react";
import template from "./SearchButton.jsx";

class SearchButton extends React.Component {
  render() {
    return template.call(this);
  }
}

export default SearchButton;
